<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sada.mapper.ProductMapper">

	<sql id="criteria">
		<if test="keyword != null">
			where (productName like concat ('%', #{keyword}, '%'))
		</if>
	</sql>

	<!-- 상품 검색 -->
	<select id="getGoodsList" resultType="com.sada.model.ProductVO">

		select productId, productName, c.cateName,
		a.cateCode, productPrice
		from sada_product a left outer join sada_bcate c on a.cateCode = c.cateCode
		<include refid="criteria"></include>
		order by productId desc
		limit #{skip}, #{amount}

	</select>

	<!-- 상품 총 개수 -->
	<select id="goodsGetTotal" resultType="int">

		select count(*) from sada_product

		<include refid="criteria"></include>

	</select>
	
		<select id="likeSelect" resultType="com.sada.model.SelectDTO">
	
	select productId, productName,productPrice, (select cateName from sada_bcate where sada_product.cateCode = sada_bcate.cateCode) as cateName
	from sada_product	
	
	</select>

</mapper>